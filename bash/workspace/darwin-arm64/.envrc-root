#!/bin/bash
export GITHUB_WORKSPACE=$(git rev-parse --show-toplevel)
export PATH="${HOME}/.local/bin:${PATH}"
export OS="darwin"
export ARCH="arm64"
export EDITOR="code --wait"
source "${GITHUB_WORKSPACE}/bash/workspace/base/.envrc-root"
